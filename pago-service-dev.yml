server:
  port: ${CUSTOM.SERVER.PORT:9090}

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mitocode_db
    username: postgres
    password: robin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  swagger-ui:
    path: /swagger-ui.html